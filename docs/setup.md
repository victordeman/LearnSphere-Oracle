# Setup Instructions

1. Clone the repository.
2. Run `scripts/setup.sh` to generate the structure.
3. Install dependencies: `pip install -r backend/requirements.txt` and `npm install` in `frontend/`.
4. Start services with `docker-compose up`.
